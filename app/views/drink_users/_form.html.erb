<%= form_for(drink_user) do |f| %>
  <% if drink_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(drink_user.errors.count, "error") %> prohibited this drink_user from being saved:</h2>

      <ul>
      <% drink_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
